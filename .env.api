VITE_API_TARGET=http://heavy-rental-rest-api:8080

# Stripe publishable key (pk_test_...) for the real-backend deposit-payment flow
# (Spec-stripe-payment-checkout.md). Never commit a real value here — it's not a
# secret in the cryptographic sense, but this org's tooling flags it in-repo anyway.
# Put the real value in a gitignored .env.api.local (already covered by *.local in
# .gitignore) for local `npm run dev:api` testing; CI/deploy inject it from GitHub
# Secrets at build time.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_51U0cYS3czsg5HEQdzR566XXIoS2cbXfogbIk5m5xiN4Prprs3N7RgzAnnUrLzBbzIN6xrrsYbfIIMs5XxSOWon3p00TMm4TYI8
