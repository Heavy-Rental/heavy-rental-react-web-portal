services:
  portal:
    image: {{ portal_image }}
    ports:
      - "80:80"
    mem_limit: 256m
    cpus: "0.5"
    restart: unless-stopped
    volumes:
      - ./nginx-default.conf:/etc/nginx/conf.d/default.conf:ro
