# Changelog

This is the portal-level log. Feature history lives in each file under `specification/` (Change Log section). Pipeline decisions live in `adr/` and `openspec/`.

## Unreleased

### Documentation

- Aligned README, OpenSpec, and product SDD with the running app: demo logins, API vs mock auth, `/api/assets`, Stripe/Release `--mode api`, Academy/paid CD (ADR-0006), checkout copy, and retired Pricing / Rental Plan Detail surfaces.

## Notes

- The previous generic "v1.0.0 Best-README-Template" changelog did not describe this project and was replaced.
- `package.json` version remains `0.0.0` (private app); GitHub Releases are created by the Release pipeline, not by this file.
